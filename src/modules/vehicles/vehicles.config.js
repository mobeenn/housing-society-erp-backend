/**
 * Vehicle module configuration (SRS Section 19)
 * - Vehicle types and statuses
 */

const VEHICLE_TYPES = ["Car", "Bike", "Van", "Truck", "SUV", "Other"];

const VEHICLE_STATUSES = ["Active", "Blocked", "Expired"];

module.exports = {
  VEHICLE_TYPES,
  VEHICLE_STATUSES,
};
